name: flutter_problematico_catalog
version: 1.0.0+1
publish_to: 'none'
description: Projeto Flutter propositalmente problemático para aula sobre cache, latência e responsividade.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  cached_network_image: ^3.4.1
  dio: ^5.8.0+1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
